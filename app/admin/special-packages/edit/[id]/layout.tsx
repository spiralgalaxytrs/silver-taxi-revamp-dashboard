
const metadata = {
    title: 'Edit Package',
    description: 'Edit Package',
}

export default function EditLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            {children}
        </div>
    );
}
