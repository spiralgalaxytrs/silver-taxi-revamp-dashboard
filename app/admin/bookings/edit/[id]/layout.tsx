
const metadata = {
    title: 'Edit Booking',
    description: 'Edit Booking',
}

export default function EditLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            {children}
        </div>
    );
}
